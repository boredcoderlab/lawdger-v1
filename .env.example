# Non-secret config — committed, safe to share
LLM_PROVIDER=gemini
LLM_BASE_URL=http://localhost:11434/v1
LLM_MODEL=llama3.1:8b

# Secrets — DO NOT commit. Copy to .env.local and fill in.
# DATABASE_URL=postgresql://...
# DIRECT_URL=postgresql://...
# AUTH_SECRET=
# GOOGLE_API_KEY=
